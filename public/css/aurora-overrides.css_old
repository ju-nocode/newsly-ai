/* ================================================
   AURORA EFFECT - OVERRIDES
   ================================================

   L'effet Aurora est maintenant un gradient animé
   directement dans le texte (background-clip: text).

   Plus besoin de blobs en arrière-plan !

   Ce fichier garantit juste que tous les éléments
   interactifs restent cliquables.
================================================ */

/* Ensure all form elements remain fully interactive */
.modal-overlay,
.modal-container,
.auth-form,
.form-input,
.form-label,
.btn-submit,
.btn-primary,
button,
input,
select,
textarea {
  pointer-events: auto !important;
  user-select: auto !important;
  -webkit-user-select: auto !important;
}

/* Ensure modal is always on top and interactive */
.modal-overlay {
  z-index: 20000 !important;
  pointer-events: auto !important;
}

.modal-container {
  z-index: 20001 !important;
  pointer-events: auto !important;
}

/* Le texte Aurora reste sélectionnable */
.aurora-text {
  user-select: text !important;
  -webkit-user-select: text !important;
  cursor: inherit !important;
}
